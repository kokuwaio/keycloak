commonLabels:
  app.kubernetes.io/name: fluent-bit

resources:
  - rbac.yaml
  - deamonset.yaml

configMapGenerator:
  - name: fluent-bit-config
    files:
      - files/fluent-bit.conf
      - files/parsers.conf
