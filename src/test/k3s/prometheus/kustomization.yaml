commonLabels:
  app.kubernetes.io/name: prometheus

resources:
  - rbac.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

configMapGenerator:
  - name: prometheus
    files: [files/config.yaml]
